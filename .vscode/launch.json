{
    "configurations": [
        {
            "name": "Launch Chrome",
            "request": "launch",
            "type": "chrome",
            "url": "http://localhost:1313",
            "preLaunchTask": "Dev Server",
            "webRoot": "${workspaceFolder}"
        }
    ]
}
